# compose.yaml
# VictoriaMetrics 集群 + Grafana (Keycloak OIDC) + vmauth (JWT读 / Basic Auth写)
# 环境变量引用 .env 文件
# 参考:
#   - https://github.com/VictoriaMetrics/VictoriaMetrics/blob/master/deployment/docker/compose-vm-cluster.yml
#   - https://docs.victoriametrics.com/guides/grafana-vmauth-openid-configuration/

x-healthcheck-defaults: &healthcheck-defaults
  interval: 10s
  timeout: 5s
  retries: 12
  start_period: 30s

x-resource-grafana: &resource-grafana
  cpus: "${GRAFANA_CPUS:-0.50}"
  mem_limit: "${GRAFANA_MEM_LIMIT:-512m}"
  mem_reservation: "${GRAFANA_MEM_RESERVATION:-192m}"
  pids_limit: 512

x-resource-keycloak: &resource-keycloak
  cpus: "${KEYCLOAK_CPUS:-0.75}"
  mem_limit: "${KEYCLOAK_MEM_LIMIT:-1g}"
  mem_reservation: "${KEYCLOAK_MEM_RESERVATION:-384m}"
  pids_limit: 512

x-resource-vmstorage: &resource-vmstorage
  cpus: "${VMSTORAGE_CPUS:-0.90}"
  mem_limit: "${VMSTORAGE_MEM_LIMIT:-1536m}"
  mem_reservation: "${VMSTORAGE_MEM_RESERVATION:-768m}"
  pids_limit: 512

x-resource-vminsert: &resource-vminsert
  cpus: "${VMINSERT_CPUS:-0.35}"
  mem_limit: "${VMINSERT_MEM_LIMIT:-256m}"
  mem_reservation: "${VMINSERT_MEM_RESERVATION:-64m}"
  pids_limit: 256

x-resource-vmselect: &resource-vmselect
  cpus: "${VMSELECT_CPUS:-0.60}"
  mem_limit: "${VMSELECT_MEM_LIMIT:-768m}"
  mem_reservation: "${VMSELECT_MEM_RESERVATION:-256m}"
  pids_limit: 512

x-resource-vmauth: &resource-vmauth
  cpus: "${VMAUTH_CPUS:-0.30}"
  mem_limit: "${VMAUTH_MEM_LIMIT:-256m}"
  mem_reservation: "${VMAUTH_MEM_RESERVATION:-64m}"
  pids_limit: 256

x-resource-vmagent: &resource-vmagent
  cpus: "${VMAGENT_CPUS:-0.40}"
  mem_limit: "${VMAGENT_MEM_LIMIT:-384m}"
  mem_reservation: "${VMAGENT_MEM_RESERVATION:-128m}"
  pids_limit: 256

x-resource-vmalert: &resource-vmalert
  cpus: "${VMALERT_CPUS:-0.40}"
  mem_limit: "${VMALERT_MEM_LIMIT:-384m}"
  mem_reservation: "${VMALERT_MEM_RESERVATION:-128m}"
  pids_limit: 256

x-resource-alertmanager: &resource-alertmanager
  cpus: "${ALERTMANAGER_CPUS:-0.20}"
  mem_limit: "${ALERTMANAGER_MEM_LIMIT:-128m}"
  mem_reservation: "${ALERTMANAGER_MEM_RESERVATION:-64m}"
  pids_limit: 128

services:
  # ============================================================
  # Grafana - 可视化面板
  # ============================================================
  grafana:
    <<: *resource-grafana
    image: grafana/grafana:${GRAFANA_VERSION:-12.4.0}
    user: "${CONTAINER_USER_ID:-1001}:${CONTAINER_GROUP_ID:-1001}"
    restart: unless-stopped
    depends_on:
      vmauth:
        condition: service_healthy
    ports:
      - "${GRAFANA_PORT:-3000}:3000"
    environment:
      GF_SERVER_ROOT_URL: http://${HOST_IP}:${GRAFANA_PORT:-3000}
      GF_SECURITY_ADMIN_PASSWORD: ${GF_ADMIN_PASS:-admin}
      GF_USERS_DEFAULT_LANGUAGE: zh-Hans
      GF_DATE_FORMATS_DEFAULT_WEEK_START: monday
      # ---------- OIDC ----------
      GF_AUTH_GENERIC_OAUTH_ENABLED: true
      GF_AUTH_GENERIC_OAUTH_ALLOW_SIGN_UP: true
      GF_AUTH_GENERIC_OAUTH_NAME: keycloak
      GF_AUTH_GENERIC_OAUTH_CLIENT_ID: grafana
      GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET: ${GRAFANA_CLIENT_SECRET}
      GF_AUTH_GENERIC_OAUTH_EMAIL_ATTRIBUTE_PATH: email
      GF_AUTH_GENERIC_OAUTH_LOGIN_ATTRIBUTE_PATH: username
      GF_AUTH_GENERIC_OAUTH_NAME_ATTRIBUTE_PATH: full_name
      GF_AUTH_GENERIC_OAUTH_SCOPES: openid profile email
      GF_AUTH_GENERIC_OAUTH_USE_REFRESH_TOKEN: true
      GF_AUTH_GENERIC_OAUTH_AUTH_URL: http://${HOST_IP}:${KC_PORT:-3001}/realms/${KC_REALM:-grafana}/protocol/openid-connect/auth
      GF_AUTH_GENERIC_OAUTH_TOKEN_URL: http://${HOST_IP}:${KC_PORT:-3001}/realms/${KC_REALM:-grafana}/protocol/openid-connect/token
      GF_AUTH_GENERIC_OAUTH_API_URL: http://${HOST_IP}:${KC_PORT:-3001}/realms/${KC_REALM:-grafana}/protocol/openid-connect/userinfo
      GF_AUTH_GENERIC_OAUTH_SIGNOUT_REDIRECT_URL: http://${HOST_IP}:${KC_PORT:-3001}/realms/${KC_REALM:-grafana}/protocol/openid-connect/logout?post_logout_redirect_uri=http://${HOST_IP}:${GRAFANA_PORT:-3000}/login
      # 角色映射: role-grafanaAdmin -> GrafanaAdmin, role-admin -> Admin, role-editor -> Editor, 其他 -> Viewer
      GF_AUTH_GENERIC_OAUTH_ROLE_ATTRIBUTE_PATH: contains(groups[*], 'role-grafanaAdmin') && 'GrafanaAdmin' || contains(groups[*], 'role-admin') && 'Admin' || contains(groups[*], 'role-editor') && 'Editor' || 'Viewer'
    volumes:
      - ./grafana/data:/var/lib/grafana/
    healthcheck:
      <<: *healthcheck-defaults
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:3000/api/health"]

  # ============================================================
  # Keycloak - OIDC 身份提供商
  # ============================================================
  keycloak:
    <<: *resource-keycloak
    image: quay.io/keycloak/keycloak:${KEYCLOAK_VERSION:-26.6.1}
    command:
      - start-dev
      - --http-port=3001
    ports:
      - "${KC_PORT:-3001}:3001"
    environment:
      KC_HOSTNAME_BACKCHANNEL_DYNAMIC: "true"
      KC_HOSTNAME: http://${HOST_IP}:${KC_PORT:-3001}/
      KC_HEALTH_ENABLED: "true"
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KC_BOOTSTRAP_ADMIN_USER:-admin-temporay}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KC_BOOTSTRAP_ADMIN_PASS}
    volumes:
      - ./keycloak/data:/opt/keycloak/data
    healthcheck:
      <<: *healthcheck-defaults
      start_period: 60s
      # Source: https://www.keycloak.org/observability/health#_healthcheck
      test: ["CMD-SHELL", "{ printf 'HEAD /health/ready HTTP/1.0\r\n\r\n' >&0; grep 'HTTP/1.0 200'; } 0<>/dev/tcp/localhost/9000"]

  # ============================================================
  # VictoriaMetrics Cluster - 存储节点
  # ============================================================
  vmstorage-1:
    <<: *resource-vmstorage
    image: victoriametrics/vmstorage:${VICTORIAMETRICS_VERSION:-v1.142.0}-cluster
    user: "${CONTAINER_USER_ID:-1001}:${CONTAINER_GROUP_ID:-1001}"
    restart: unless-stopped
    volumes:
      - ./vmstorage-1/data:/storage
    command:
      - --storageDataPath=/storage
      - --retentionPeriod=${VM_RETENTION_PERIOD:-90d}
    healthcheck:
      <<: *healthcheck-defaults
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:8482/health"]

  # ============================================================
  # VictoriaMetrics Cluster - 写入前端
  # ============================================================
  vminsert-1:
    <<: *resource-vminsert
    image: victoriametrics/vminsert:${VICTORIAMETRICS_VERSION:-v1.142.0}-cluster
    user: "${CONTAINER_USER_ID:-1001}:${CONTAINER_GROUP_ID:-1001}"
    restart: unless-stopped
    depends_on:
      vmstorage-1:
        condition: service_healthy
    volumes:
      - ./vminsert-1/data:/data
    command:
      - --storageNode=vmstorage-1:8400
    healthcheck:
      <<: *healthcheck-defaults
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:8480/health"]

  # ============================================================
  # VictoriaMetrics Cluster - 查询前端
  # ============================================================
  vmselect-1:
    <<: *resource-vmselect
    image: victoriametrics/vmselect:${VICTORIAMETRICS_VERSION:-v1.142.0}-cluster
    user: "${CONTAINER_USER_ID:-1001}:${CONTAINER_GROUP_ID:-1001}"
    restart: unless-stopped
    depends_on:
      vmstorage-1:
        condition: service_healthy
    volumes:
      - ./vmselect-1/data:/data
    command:
      - --storageNode=vmstorage-1:8401
      - --vmalert.proxyURL=http://vmalert:8880
    healthcheck:
      <<: *healthcheck-defaults
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:8481/health"]

  # ============================================================
  # vmauth - 统一认证代理（读: JWT/OIDC, 写: Basic Auth）
  # ============================================================
  vmauth:
    <<: *resource-vmauth
    image: victoriametrics/vmauth:${VICTORIAMETRICS_VERSION:-v1.142.0}
    user: "${CONTAINER_USER_ID:-1001}:${CONTAINER_GROUP_ID:-1001}"
    restart: unless-stopped
    depends_on:
      vmselect-1:
        condition: service_healthy
    ports:
      - "${VMAUTH_PORT:-8427}:8427"
    volumes:
      - ./vmauth/auth.yaml:/etc/auth.yaml
      - ./vmauth/data:/data
    command:
      - --auth.config=/etc/auth.yaml
    healthcheck:
      <<: *healthcheck-defaults
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:8427/health"]

  # ============================================================
  # vmagent - 指标抓取（采集 VM 集群自身指标）
  # ============================================================
  vmagent:
    <<: *resource-vmagent
    image: victoriametrics/vmagent:${VICTORIAMETRICS_VERSION:-v1.142.0}
    user: "${CONTAINER_USER_ID:-1001}:${CONTAINER_GROUP_ID:-1001}"
    restart: unless-stopped
    depends_on:
      vmauth:
        condition: service_healthy
    ports:
      - "${VMAGENT_PORT:-8429}:8429"
    volumes:
      - ./vmagent/scrape.yaml:/etc/vmagent/config.yaml
      - ./vmagent/data:/vmagentdata
    command:
      - --promscrape.config=/etc/vmagent/config.yaml
      - --remoteWrite.url=http://vmauth:8427/insert/0/prometheus/api/v1/write
      - --remoteWrite.basicAuth.username=vmadmin
      - --remoteWrite.basicAuth.password=${VMADMIN_PASS:-vmadmin_pass}
      - --remoteWrite.tmpDataPath=/vmagentdata/remotewrite
    healthcheck:
      <<: *healthcheck-defaults
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:8429/health"]

  # ============================================================
  # vmalert - 告警规则引擎
  # ============================================================
  vmalert:
    <<: *resource-vmalert
    image: victoriametrics/vmalert:${VICTORIAMETRICS_VERSION:-v1.142.0}
    user: "${CONTAINER_USER_ID:-1001}:${CONTAINER_GROUP_ID:-1001}"
    restart: unless-stopped
    depends_on:
      alertmanager:
        condition: service_healthy
      vmauth:
        condition: service_healthy
    ports:
      - "${VMALERT_PORT:-8880}:8880"
    volumes:
      - ./vmalert/rules:/etc/alerts
      - ./vmalert/data:/data
    command:
      - --datasource.url=http://vmauth:8427/select/0/prometheus
      - --datasource.basicAuth.username=vmadmin
      - --datasource.basicAuth.password=${VMADMIN_PASS:-vmadmin_pass}
      - --remoteRead.url=http://vmauth:8427/select/0/prometheus
      - --remoteRead.basicAuth.username=vmadmin
      - --remoteRead.basicAuth.password=${VMADMIN_PASS:-vmadmin_pass}
      - --remoteWrite.url=http://vmauth:8427/insert/0/prometheus/api/v1/write
      - --remoteWrite.basicAuth.username=vmadmin
      - --remoteWrite.basicAuth.password=${VMADMIN_PASS:-vmadmin_pass}
      - --notifier.url=http://alertmanager:9093/
      - --rule=/etc/alerts/*.yml
      - --external.url=http://${HOST_IP}:${GRAFANA_PORT:-3000}
    healthcheck:
      <<: *healthcheck-defaults
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:8880/health"]

  # ============================================================
  # alertmanager - 告警通知
  # ============================================================
  alertmanager:
    <<: *resource-alertmanager
    image: prom/alertmanager:${ALERTMANAGER_VERSION:-v0.28.1}
    restart: unless-stopped
    # 不暴露外部端口，内部通过 vmalert 访问
    volumes:
      - ./alertmanager/config:/config
    command:
      - --config.file=/config/alertmanager.yml
    healthcheck:
      <<: *healthcheck-defaults
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:9093/-/ready"]
